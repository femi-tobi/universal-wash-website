PORT=3000
JWT_SECRET=change_this_to_a_long_random_string
NODE_ENV=production

# ── PostgreSQL (Render / hosted) ─────────────────────────────────────────────
# Render automatically sets DATABASE_URL when you link a PostgreSQL service.
# Copy the "Internal Database URL" from your Render Postgres dashboard.
DATABASE_URL=postgres://user:password@host:5432/dbname

# ── MySQL (self-hosted / local) ──────────────────────────────────────────────
# Used only when DATABASE_URL is NOT set and a MySQL server is reachable.
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=laundry_db

# ── SQLite ───────────────────────────────────────────────────────────────────
# Automatic fallback when neither PostgreSQL nor MySQL is available.
# No extra configuration needed.
